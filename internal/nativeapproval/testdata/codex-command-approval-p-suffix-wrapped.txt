  Would you like to run the following command?

  Environment: local

  Reason: Разрешить один локальный read-only probe для проверки тестового проекта?

  $ example inspect --project demo --read-only

› 1. Yes, proceed (y)
  2. Yes, and don't ask again for commands that start with `example inspect --project demo --read-only`
     (p)
  3. No, and tell Codex what to do differently (esc)

  Press enter to confirm or esc to cancel
